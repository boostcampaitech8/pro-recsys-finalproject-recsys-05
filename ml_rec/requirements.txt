# ============================================
# BentoML 추천 서비스 프로덕션 환경
# ============================================

# 필수: 모델 추론
bentoml==1.3.11
torch==2.1.0
xgboost
numpy==1.24.3
pandas==2.0.3
pyyaml==6.0

# 필수: 데이터 처리
scipy==1.11.2
scikit-learn==1.3.0

# 버전 호환성
pyparsing>=2.4.7,<3.1.0

# 선택: GCS 다운로드 (모델/데이터 관리용)
google-cloud-storage

# ============================================
# 제거 사항 (개발/훈련용 - 프로덕션 불필요):
# - recbole: 모델 훈련용 (프로덕션 불필요)
# - lightgbm: 훈련용 (프로덕션 불필요)
# - polars: 데이터 처리 최적화 (pandas로 충분)
# - matplotlib: 시각화 (프로덕션 불필요)
# - seaborn: 시각화 (프로덕션 불필요)
# - wandb: 실험 추적 (프로덕션 불필요)
# - tqdm: 진행률 표시 (프로덕션 불필요)
# ============================================