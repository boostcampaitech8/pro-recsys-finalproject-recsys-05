# Git
.git
.gitignore

# Virtual Environments
.venv
venv
env
backend/.venv
backend/venv
**/venv
**/.venv
**/env

# Python Cache
__pycache__
**/*.pyc
**/*.pyo
**/*.pyd
**/.pytest_cache
.pytest_cache

# Pytest / Coverage
.coverage
.coverage.*
nosetests.xml
coverage.xml
*.cover

# Logs
*.log

# Large Files / Models (Check specific folders if needed)
**/*.pt
**/*.pth
**/*.bin
**/*.onnx
app/data/games_metadata.jsonl  # Add if this file is generated/not needed in the production image. But currently may be needed.
# If games_metadata.jsonl is needed, keep it. But if it's large and can be downloaded/generated, consider excluding.
# Actually, it's 200MB, so it might stay if crucial. But .venv is ~GBs.

# Other
Dockerfile
README.md
backend/README.md