# 필요한 예시를 복사해서 사용하세요:
# - 백엔드: configs/backend/.env.example -> configs/backend/.env (로컬/도커 공통)
# - 프론트: configs/frontend/.env.example -> configs/frontend/.env
#
# 참고: 루트 .env는 Docker Compose 변수 치환용으로만 사용됩니다.
